package org.spring.metaquery.dto.schema;

/**
 * SchemaInitResponse is a data transfer object that represents the response
 * for schema initialization requests. It contains the schema as a string.
 */
public record SchemaInitResponse(String schema) {
}
